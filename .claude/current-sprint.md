# 当前 Sprint

本文档记录当前正在进行的开发。包括：当前 Sprint 的各项任务（每个 Sprint 开始时从 sprint-plan 继承过来并进行更新，每个 Sprint 结束后回写到 sprint-plan）；包括各模块的当前状态（待开始、改动中、稳定、冻结），最后更新时间，当前目标（极简要的总结性说明），备注；包括正在修改的活跃文件清单（以清单形式列出，及时更新，以防止冲突）；包括近期重要改动记录（包括修改时间，改动目的，主要涉及的模块和文件）。

**存档：** 每个 Sprint 结束后，此文件的内容需要被完整复制到 `.claude/archive` 目录下，以当前 Sprint 的名称命名的新 md 文件中进行留存。同时，把本文档的内容按照下一个计划中的 Sprint 进行初始化。

**同步：** 任务表中责任人标记为"人工"的任务，须与根目录 `人工TODO事项.md` 双向同步。人工完成事项后应同时更新两处状态。

---

> 等待 Sprint 0 启动后填充。
